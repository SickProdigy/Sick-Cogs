# Incidents

A staff-only reporting view over Red's existing ModLog cases. It does not create a second ban database or expose cases to ordinary members.

```text
[p]incidents recent [limit]
[p]incidents member @member
[p]incidents summary [days]
```

Load Red's `modlog` and normal moderation cogs first; they remain responsible for recording cases.
